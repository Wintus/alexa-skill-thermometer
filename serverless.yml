service:
  name: alexa-skill-thermometer

plugins:
  - serverless-webpack

provider:
  name: aws
  runtime: nodejs8.10
  region: ap-northeast-1

custom:
  webpack:
    packager: yarn

functions:
  hello:
    handler: handler.hello
    events:
      - http:
          method: get
          path: hello
